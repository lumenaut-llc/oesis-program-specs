# v1.0 Example Deltas

This directory holds additive `v1.0` example payloads or overrides.

The canonical baseline `../../v0.1/examples/` directory remains the frozen
`v0.1` default example surface. Add files here only when a future-lane example
must differ from that baseline.

Current `v1.0` additive example files:

- `equipment-state-observation.example.json`
- `source-provenance-record.example.json`
- `consent-record.example.json`
- `consent-store.example.json`
- `sharing-settings.example.json`
- `sharing-store.example.json`
