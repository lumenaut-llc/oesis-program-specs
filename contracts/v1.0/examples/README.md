# v1.0 Example Deltas

This directory holds additive `v1.0` example payloads or overrides.

The root `../../examples/` directory remains the frozen `v0.1` default example
surface. Add files here only when a future-lane example must differ from that
baseline.
