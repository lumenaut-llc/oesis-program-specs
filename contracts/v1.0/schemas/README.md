# v1.0 Schema Deltas

This directory holds additive `v1.0` schema files or schema overrides.

The root `../../schemas/` directory remains the frozen `v0.1` default schema
surface. Add files here only when a future-lane contract must differ from that
baseline.
