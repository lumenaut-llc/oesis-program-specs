# v0.2 Schemas

No `v0.2`-specific schema deltas are currently defined.

Use `../../v0.1/schemas/` as the canonical source until an explicit `v0.2`
override is accepted.

Status: complete scaffold for inheritance lane.
