# Contracts v0.2

`v0.2` is a promotion lane marker for the first widened parcel kit.

## Contract posture

- Inherit frozen baseline contracts from `../v0.1/`.
- Add files here only if a `v0.2`-specific contract delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## Structure

- `schemas/` — placeholder index for future `v0.2` schema overrides
- `examples/` — placeholder index for future `v0.2` example overrides

## How to use this lane

- For current work, use `../v0.1/schemas/` and `../v0.1/examples/`.
- Add files in this lane only when a concrete `v0.2` contract delta is accepted.
- Do not copy `v0.1` files here by default.

## Completion status

- Lane marker README: complete
- `examples/` scaffold: complete
- `schemas/` scaffold: complete
- Lane-specific contract overrides: none (inherits baseline `v0.1`)

## Related

- `../v0.1/README.md`
- `schemas/README.md`
- `examples/README.md`
- `../../release/v0.2/README.md`
