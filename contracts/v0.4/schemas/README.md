# v0.4 Schemas

No `v0.4`-specific schema deltas are currently defined.

Use `../../v0.1/schemas/` as the canonical source until an explicit `v0.4`
override is accepted.

Status: complete scaffold for inheritance lane.
