# Contracts v0.4

`v0.4` is a promotion lane marker for multi-node registry and evidence
composition progression.

## Contract posture

- Inherit frozen baseline contracts from `../v0.1/`.
- Add files here only if a `v0.4`-specific contract delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## Structure

- `schemas/` — placeholder index for future `v0.4` schema overrides
- `examples/` — placeholder index for future `v0.4` example overrides

## How to use this lane

- For current work, use `../v0.1/schemas/` and `../v0.1/examples/`.
- Add files in this lane only when a concrete `v0.4` contract delta is accepted.
- Do not copy `v0.1` files here by default.

## Completion status

- Lane marker README: complete
- `examples/` scaffold: complete
- `schemas/` scaffold: complete
- Lane-specific contract overrides: none (inherits baseline `v0.1`)

## Related

- `../v0.1/README.md`
- `schemas/README.md`
- `examples/README.md`
- `../../release/v0.4/README.md`
