# `v1.5` schemas

Put bridge-stage schema deltas here when a machine-readable contract needs to be
more specific than the root `v0.1` baseline or the broader `v1.0` lane.

Current additions:

- `house-state.schema.json`
- `intervention-event.schema.json`
- `verification-outcome.schema.json`
- `equipment-state-observation.schema.json`
- `source-provenance-record.schema.json`
