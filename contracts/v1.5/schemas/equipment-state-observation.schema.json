{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lumenaut-llc.github.io/oesis-program-specs/contracts/v1.5/schemas/equipment-state-observation.schema.json",
  "title": "OESIS Equipment State Observation v1.5",
  "type": "object",
  "required": [
    "parcel_id",
    "captured_at",
    "confidence_band",
    "source",
    "signals"
  ],
  "additionalProperties": false,
  "properties": {
    "parcel_id": {
      "type": "string",
      "minLength": 1
    },
    "captured_at": {
      "type": "string",
      "format": "date-time"
    },
    "confidence_band": {
      "type": "string",
      "enum": ["high", "medium", "low", "none"]
    },
    "bridge_role": {
      "type": "string",
      "enum": ["house_state_support", "verification_support", "both"]
    },
    "source": {
      "type": "object",
      "required": ["source_kind", "source_name", "method", "ttl_seconds"],
      "additionalProperties": false,
      "properties": {
        "source_kind": {
          "type": "string",
          "enum": [
            "direct_measurement",
            "adapter_derived",
            "inferred",
            "manual_entry"
          ]
        },
        "source_name": {
          "type": "string",
          "minLength": 1
        },
        "method": {
          "type": "string",
          "minLength": 1
        },
        "ttl_seconds": {
          "type": "integer",
          "minimum": 0
        },
        "raw_ref": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "signals": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "hvac_mode": {
          "type": "string",
          "enum": [
            "off",
            "cool",
            "heat",
            "auto",
            "fan_only",
            "recirculate",
            "unknown"
          ]
        },
        "fan_state": { "type": "string", "enum": ["on", "off", "auto", "unknown"] },
        "air_source_mode": {
          "type": "string",
          "enum": ["recirculate", "fresh_air", "mixed", "unknown"]
        },
        "purifier_state": { "type": "string", "enum": ["on", "off", "auto", "unknown"] },
        "sump_state": {
          "type": "string",
          "enum": ["standby", "starting", "running", "overload", "unknown"]
        },
        "outdoor_air_intake": { "type": "boolean" },
        "equipment_running": { "type": "boolean" },
        "setpoint_f": { "type": "number" }
      }
    }
  }
}
