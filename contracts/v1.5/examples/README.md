# `v1.5` examples

Put bridge-stage example payload deltas here when examples need to show
house-state, intervention, verification, or response-metadata behavior that
goes beyond the root `v0.1` baseline.

Do not rewrite the baseline examples in `../../v0.1/examples/` when the change is
specific to the `v1.5` bridge lane.

Current additions:

- `house-state.example.json`
- `intervention-event.example.json`
- `verification-outcome.example.json`
- `equipment-state-observation.example.json`
- `source-provenance-record.example.json`
