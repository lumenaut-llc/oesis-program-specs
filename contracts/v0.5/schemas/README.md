# v0.5 Schemas

No `v0.5`-specific schema deltas are currently defined.

Use `../../v0.1/schemas/` as the canonical source until an explicit `v0.5`
override is accepted.

Status: complete scaffold for inheritance lane.
