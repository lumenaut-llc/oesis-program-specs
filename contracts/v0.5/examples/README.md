# v0.5 Examples

No `v0.5`-specific example deltas are currently defined.

Use `../../v0.1/examples/` as the canonical source until an explicit `v0.5`
override is accepted.

Status: complete scaffold for inheritance lane.
