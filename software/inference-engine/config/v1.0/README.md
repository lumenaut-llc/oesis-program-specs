# Inference Config v1.0

This directory holds additive `v1.0` inference-config overrides for the future
runtime lane.

The parent directory remains the frozen `v0.1` default config surface. If a
future-lane config file is not present here, the `v0.1` baseline remains the
fallback until an explicit `v1.0` override is added.
