# Software v0.4

`v0.4` is a promotion lane marker for multi-node registry and evidence composition.

## Software posture

- Inherit baseline software docs from `../v0.1/`.
- Add files here only if a `v0.4`-specific software delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.4 means for software

- Node registry lifecycle management (register, bind, disable, replace, retire)
- Installation metadata capture as structured input
- Evidence composition weighting across heterogeneous observation families
- Deployment-quality flags distinguishing bench-grade from field-ready

## How to use this lane

- For current work, use `../v0.1/` and the root-level subsystem directories.
- Add files in this lane only when a concrete `v0.4` software delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.3/README.md`
- `../../release/v0.4/README.md`
- `../../contracts/v1.0/deployment-metadata-schema.md`
