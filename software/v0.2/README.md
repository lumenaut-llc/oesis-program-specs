# Software v0.2

`v0.2` is a promotion lane marker for the first widened parcel kit.

## Software posture

- Inherit baseline software docs from `../v0.1/`.
- Add files here only if a `v0.2`-specific software delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.2 means for software

- `mast-lite` normalization in the ingest service (same packet lineage as bench-air)
- Multi-source evidence composition in the inference engine
- Parcel view distinguishes indoor vs sheltered outdoor evidence sources
- Field-hardening alignment in the software stack

## How to use this lane

- For current work, use `../v0.1/` and the root-level subsystem directories.
- Add files in this lane only when a concrete `v0.2` software delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.1/README.md`
- `../../release/v0.2/README.md`
- `../../architecture/current/pre-1.0-version-progression.md`
