# Software v0.5

`v0.5` is a promotion lane marker for operational governance enforcement.

## Software posture

- Inherit baseline software docs from `../v0.1/`.
- Add files here only if a `v0.5`-specific software delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.5 means for software

- Consent enforcement as a runtime gate on sharing paths
- Revocation that reliably stops future sharing
- Retention cleanup with operational owners and schedule
- Export bundle as a complete, auditable product surface
- Operator access logging as operational discipline
- Sharing settings as a user-facing surface

## How to use this lane

- For current work, use `../v0.1/` and the root-level subsystem directories.
- Add files in this lane only when a concrete `v0.5` software delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.4/README.md`
- `../../release/v0.5/README.md`
- `../../contracts/v1.0/governance-operational-model.md`
