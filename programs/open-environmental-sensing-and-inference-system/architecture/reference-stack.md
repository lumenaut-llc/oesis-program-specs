# Reference Stack

## Status

Transitional pointer document.

## Purpose

The canonical current reference-stack map now lives at:

- `../technical-architecture/v0.1/reference-stack.md`

For the first functioning `v0.1` slice, also read:

- `../technical-architecture/v0.1/minimum-functioning-v0.1.md`

That file clarifies that the first meaning of network in `v0.1` is
collection into the home/platform ingest boundary, not mature neighborhood
intelligence.

For the versioned architecture entrypoint, use:

- `../technical-architecture/README.md`
