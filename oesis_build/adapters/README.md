# Build Adapters

Adapter modules that connect the build system to external sources.

- `canonical_sources.py` — Resolves canonical file paths across repos
- `repo_links.py` — Generates cross-repo reference links
