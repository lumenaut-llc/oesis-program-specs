# Build Model

Data model classes used by the build system.

- `artifact.py` — Represents a build artifact (schema, example, bundle)
- `publication.py` — Tracks publication state and targets
- `release_manifest.py` — Release manifest structure and serialization
