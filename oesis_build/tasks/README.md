# Build Tasks

Invoke-based task modules for the OESIS build system.

- `base.py` — Core build tasks (clean, lint, validate)
- `release.py` — Release packaging and manifest generation
- `site.py` — Public site build tasks
- `validation.py` — Schema and example validation tasks
