# Hardware v0.4

`v0.4` is a promotion lane marker for multi-node registry and evidence composition.

## Hardware posture

- Inherit baseline hardware docs from `../v0.1/`.
- Add files here only if a `v0.4`-specific hardware delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.4 means for hardware

- Multi-node installation metadata captured as structured input per node family
- Deployment maturity labels enforced per node family
- Hardware lifecycle states (register, bind, disable, replace) documented

## How to use this lane

- For current work, use `../v0.1/` and the root-level node family directories.
- Add files in this lane only when a concrete `v0.4` hardware delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.3/README.md`
- `../../release/v0.4/README.md`
- `../../architecture/system/deployment-maturity-ladder.md`
