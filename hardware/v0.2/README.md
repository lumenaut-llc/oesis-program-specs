# Hardware v0.2

`v0.2` is a promotion lane marker for the first widened parcel kit.

## Hardware posture

- Inherit baseline hardware docs from `../v0.1/`.
- Add files here only if a `v0.2`-specific hardware delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.2 means for hardware

- `mast-lite` promoted to accepted reference hardware alongside `bench-air-node`
- Two-node kit field-hardening requirements documented and met
- Sheltered outdoor deployment posture validated

## How to use this lane

- For current work, use `../v0.1/` and the root-level node family directories.
- Add files in this lane only when a concrete `v0.2` hardware delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.1/README.md`
- `../../release/v0.2/README.md`
- `../../architecture/current/pre-1.0-version-progression.md`
