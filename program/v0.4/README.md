# Program v0.4

`v0.4` is a promotion lane marker for multi-node registry and evidence composition.

## Program posture

- Inherit baseline program docs from `../v0.1/`.
- Add files here only if a `v0.4`-specific program delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.4 means for the program

- Program scope includes full node lifecycle management as a product surface
- Multi-node evidence composition becomes an accepted parcel-level capability
- Installation metadata is a first-class program input, not optional context

## How to use this lane

- For current work, use `../v0.1/` and `../operating-packet/`.
- Add files in this lane only when a concrete `v0.4` program delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.3/README.md`
- `../../release/v0.4/README.md`
