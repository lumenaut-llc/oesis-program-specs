# Future Architecture Redirect

## Purpose

Keep older links into the debated target-architecture lane working while the
canonical content lives in `../v1.0/` and `../system/`.

## Canonical location

Use `../v1.0/README.md` for debated lane material.
Use `../system/README.md` for canonical cross-version roadmap/spec/taxonomy docs.

Use `../decisions/debate-map.md` for cross-version doctrine and debate framing.

## Note

This directory is redirect-only. New content should not be authored here.
