# Meta v0.3

`v0.3` is a promotion lane marker for the first flood-capable runtime.

## Meta posture

- Inherit baseline meta docs from `../v0.1/`.
- Add files here only if a `v0.3`-specific planning delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.3 means for meta

- Planning and coordination for flood-capable runtime promotion
- Milestone tracking for flood observation family acceptance

## How to use this lane

- For current work, use `../v0.1/`.
- Add files in this lane only when a concrete `v0.3` planning delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.2/README.md`
- `../../release/v0.3/README.md`
