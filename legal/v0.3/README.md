# Legal v0.3

`v0.3` is a promotion lane marker for the first flood-capable runtime.

## Legal posture

- Inherit baseline legal docs from `../v0.1/`.
- Add files here only if a `v0.3`-specific legal delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.3 means for legal

- No major legal changes — flood-node operates under the same licensing and
  governance posture as existing node families

## How to use this lane

- For current work, use `../v0.1/`.
- Add files in this lane only when a concrete `v0.3` legal delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.2/README.md`
- `../../release/v0.3/README.md`
