# Legal v0.2

`v0.2` is a promotion lane marker for the first widened parcel kit.

## Legal posture

- Inherit baseline legal docs from `../v0.1/`.
- Add files here only if a `v0.2`-specific legal delta is explicitly accepted.
- Until such a delta exists, this directory is intentionally lightweight.

## What v0.2 means for legal

- No major legal changes over `v0.1` — the two-node kit operates under the same
  licensing and governance posture

## How to use this lane

- For current work, use `../v0.1/`.
- Add files in this lane only when a concrete `v0.2` legal delta is accepted.
- Do not copy `v0.1` files here by default.

## Related

- `../v0.1/README.md`
- `../../release/v0.2/README.md`
