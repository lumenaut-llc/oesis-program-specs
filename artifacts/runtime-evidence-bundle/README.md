# Runtime Evidence Bundle

Generated build logs from `make oesis-validate`, `make oesis-check`, and
`make oesis-http-check`, plus a `manifest.json` summarizing the run.

These artifacts are produced by CI and local validation and are not
committed to the repository. Use them to verify that the build and
validation pipeline completed without errors.
